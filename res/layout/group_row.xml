<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="horizontal"    
    android:padding="@dimen/group_row_padding" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/groupImage"
        android:layout_alignTop="@+id/groupImage"
        android:layout_toRightOf="@+id/groupImage"
        android:gravity="center_vertical"
        android:orientation="vertical"
        android:paddingLeft="@dimen/group_row_padding" >

        <TextView
            android:id="@+id/groupName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="left"
            android:text="Medium Text"
            android:textSize="@dimen/group_row_name"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/groupSubText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="left"
            android:text="Medium Text"
            android:textSize="@dimen/group_row_sub" />
    </LinearLayout>

    <ImageView
        android:id="@+id/groupImage"
        android:layout_width="@dimen/group_row_image"
        android:layout_height="@dimen/group_row_image"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_group" />

</RelativeLayout>